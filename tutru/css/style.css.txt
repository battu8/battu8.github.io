  :root{
    --ink:#1b191e;
    --ink-2:#28232b;
    --ink-3:#332c35;
    --paper:#ece2cd;
    --paper-2:#e2d5b8;
    --paper-edge:#c9b98a;
    --seal:#a5342b;
    --seal-dim:#7a2620;
    --brass:#c6a04c;
    --brass-dim:#8d763a;
    --text-light:#f1e8d6;
    --text-mute:#b8ac95;
    --text-dark:#2a2118;
    --jade:#51715c;
    --danger:#c0392b;
  }
  *{box-sizing:border-box;}
  html,body{margin:0;padding:0;}
  body{
    background:
      radial-gradient(ellipse at top, #322b34 0%, var(--ink) 55%),
      var(--ink);
    color:var(--text-light);
    font-family:'Be Vietnam Pro', sans-serif;
    line-height:1.5;
    min-height:100vh;
    padding-bottom:80px;
  }
  .wrap{max-width:1180px;margin:0 auto;padding:0 20px;}

  /* ===== Header ===== */
  header{
    padding:56px 20px 40px;
    text-align:center;
    position:relative;
    overflow:hidden;
    border-bottom:1px solid var(--brass-dim);
  }
  header::before{
    content:"禪";
    position:absolute;
    font-family:'Noto Serif SC', serif;
    font-size:340px;
    color:rgba(198,160,76,0.045);
    top:-90px; left:50%;
    transform:translateX(-50%);
    pointer-events:none;
    line-height:1;
  }
  .eyebrow{
    font-family:'JetBrains Mono', monospace;
    font-size:11px;
    letter-spacing:.28em;
    text-transform:uppercase;
    color:var(--brass);
    margin-bottom:14px;
  }
  h1{
    font-family:'Cormorant Garamond', serif;
    font-weight:600;
    font-size:clamp(38px,6vw,64px);
    margin:0 0 10px;
    letter-spacing:.01em;
    color:var(--text-light);
  }
  h1 em{
    font-style:italic;
    color:var(--seal);
  }
  .subtitle{
    color:var(--text-mute);
    font-size:14.5px;
    max-width:520px;
    margin:0 auto;
  }
  .byline{
    margin-top:18px;
    font-family:'JetBrains Mono', monospace;
    font-size:11.5px;
    color:var(--text-mute);
    letter-spacing:.05em;
  }
  .byline b{color:var(--brass);font-weight:600;}

  .fade-hint{opacity:.32; transition:opacity .7s ease;}
  .fade-hint.in-view{opacity:1;}

  /* ===== Tabs ===== */
  .tabs{
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:18px;
    margin:40px auto 0;
  }
  .tab-main{
    position:relative;
    min-width:230px;
    padding:20px 34px;
    border-radius:46px;
    border:2px solid var(--brass-dim);
    background:var(--ink-2);
    color:var(--text-light);
    font-family:'Cormorant Garamond', serif;
    font-weight:700;
    font-size:25px;
    cursor:pointer;
    display:flex;align-items:center;justify-content:center;
    text-align:center;
    transition:all .25s ease;
  }
  .tab-main:hover{border-color:var(--brass);transform:translateY(-2px);}
  .tab-main.active{
    background:var(--seal);
    border-color:var(--seal);
    color:var(--text-light);
    box-shadow:0 0 0 5px rgba(165,52,43,0.18), 0 12px 28px -6px rgba(165,52,43,.45);
  }
  .tabs-secondary{
    display:flex;
    align-items:center;
    gap:12px;
    flex-wrap:wrap;
    justify-content:center;
  }
  .tabs-or{
    font-family:'JetBrains Mono', monospace;
    font-size:10px;
    letter-spacing:.2em;
    text-transform:uppercase;
    color:var(--text-mute);
  }
  .tab-sub{
    padding:9px 20px;
    border-radius:30px;
    border:1px solid var(--brass-dim);
    background:transparent;
    color:var(--text-mute);
    font-family:'Be Vietnam Pro', sans-serif;
    font-weight:500;
    font-size:12.5px;
    cursor:pointer;
    transition:all .2s ease;
  }
  .tab-sub:hover{border-color:var(--brass);color:var(--brass);}
  .tab-sub.active{
    border-color:var(--brass);
    color:var(--brass);
    background:rgba(198,160,76,0.08);
  }

  /* ===== Form Card ===== */
  .card{
    max-width:760px;
    margin:36px auto 0;
    background:var(--ink-2);
    border:1px solid var(--brass-dim);
    border-radius:6px;
    padding:32px 30px;
    position:relative;
  }
  .card::before{
    content:"";
    position:absolute;inset:6px;
    border:1px solid rgba(198,160,76,0.18);
    border-radius:3px;
    pointer-events:none;
  }
  .form-title{
    font-family:'Cormorant Garamond',serif;
    font-size:22px;
    font-weight:600;
    color:var(--brass);
    margin:0 0 4px;
  }
  .form-desc{color:var(--text-mute);font-size:13px;margin:0 0 24px;}
  .grid{
    display:grid;
    grid-template-columns:repeat(auto-fit, minmax(120px,1fr));
    gap:16px;
  }
  .field label{
    display:block;
    font-family:'JetBrains Mono', monospace;
    font-size:10.5px;
    letter-spacing:.06em;
    text-transform:uppercase;
    color:var(--text-mute);
    margin-bottom:7px;
  }
  .field select, .field input{
    width:100%;
    background:var(--ink);
    border:1px solid var(--brass-dim);
    color:var(--text-light);
    padding:10px 10px;
    border-radius:4px;
    font-family:'Be Vietnam Pro', sans-serif;
    font-size:14.5px;
    outline:none;
    transition:border-color .2s;
  }
  .field select:focus, .field input:focus{border-color:var(--brass);}
  .field-full{grid-column:1/-1;}

  .radio-row{display:flex;gap:18px;flex-wrap:wrap;margin-top:6px;}
  .radio-opt{
    display:flex;align-items:center;gap:7px;
    font-size:13.5px;color:var(--text-light);
    cursor:pointer;
  }
  .radio-opt input{width:auto;accent-color:var(--seal);}

  .divider{height:1px;background:linear-gradient(90deg, transparent, var(--brass-dim), transparent);margin:26px 0;}

  .btn-main-wrap{display:flex;flex-direction:column;align-items:center;margin-top:14px;}
  .btn-main{
    width:120px;height:120px;
    border-radius:50%;
    background:var(--seal);
    color:var(--text-light);
    border:3px solid var(--brass);
    cursor:pointer;
    display:flex;align-items:center;justify-content:center;
    box-shadow:0 12px 26px -8px rgba(165,52,43,.55);
    transition:all .2s;
  }
  .btn-main svg{width:52px;height:52px;}
  .btn-main:hover{background:#bd3b30;transform:translateY(-2px) scale(1.03);}
  .btn-main:active{transform:translateY(0) scale(.98);}
  .btn-main-caption{
    margin-top:12px;
    font-family:'Cormorant Garamond',serif;
    font-weight:600;
    font-size:16px;
    color:var(--brass);
    letter-spacing:.03em;
  }

  /* ===== Loading overlay (âm dương xoay) ===== */
  .loading-overlay{
    display:none;
    position:fixed;inset:0;
    background:rgba(27,25,30,0.88);
    z-index:999;
    align-items:center;justify-content:center;
    flex-direction:column;
    gap:22px;
  }
  .loading-overlay.show{display:flex;}
  .handicon{width:88px;height:88px;}
  /* Lúc loading: ngón cái luôn ẩn, 4 ngón còn lại lần lượt ẩn rồi hiện như đang đếm */
  #loading-overlay .handicon .thumb{opacity:0;}
  #loading-overlay .handicon .finger-1{animation:fingerBlink 1.3s ease-in-out infinite;}
  #loading-overlay .handicon .finger-2{animation:fingerBlink 1.3s ease-in-out infinite;animation-delay:.18s;}
  #loading-overlay .handicon .finger-3{animation:fingerBlink 1.3s ease-in-out infinite;animation-delay:.36s;}
  #loading-overlay .handicon .finger-4{animation:fingerBlink 1.3s ease-in-out infinite;animation-delay:.54s;}
  @keyframes fingerBlink{
    0%, 100% { opacity:1; }
    50%      { opacity:.1; }
  }
  .loading-text{
    font-family:'Cormorant Garamond',serif;
    font-size:18px;
    color:var(--brass);
    letter-spacing:.05em;
  }

  .btn-ghost{
    background:transparent;
    border:1px solid var(--brass-dim);
    color:var(--brass);
    padding:11px 18px;
    border-radius:5px;
    font-family:'Be Vietnam Pro',sans-serif;
    font-size:13.5px;
    cursor:pointer;
    transition:all .2s;
  }
  .btn-ghost:hover{border-color:var(--brass);background:rgba(198,160,76,0.08);}

  /* ===== Warning banner (giờ Tý) ===== */
  .warn-box{
    display:none;
    margin-top:20px;
    background:rgba(192,57,43,0.13);
    border:1.5px solid var(--danger);
    border-radius:6px;
    padding:16px 18px;
    animation:pulseWarn 2.2s infinite;
  }
  .warn-box.show{display:block;}
  @keyframes pulseWarn{
    0%,100%{box-shadow:0 0 0 0 rgba(192,57,43,0.0);}
    50%{box-shadow:0 0 0 6px rgba(192,57,43,0.10);}
  }
  .warn-box .warn-head{
    display:flex;align-items:center;gap:8px;
    font-family:'Cormorant Garamond',serif;
    font-weight:700;font-size:17px;
    color:#ff7a6b;
    margin-bottom:8px;
  }
  .warn-box p{font-size:13px;color:var(--text-light);margin:6px 0;}
  .warn-box .radio-row{margin-top:10px;}

  /* ===== Results ===== */
  #results{display:none;margin-top:56px;}
  .result-head{text-align:center;margin-bottom:8px;}
  .result-head .eyebrow{margin-bottom:8px;}
  .result-head h2{
    font-family:'Cormorant Garamond',serif;
    font-size:32px;font-weight:600;margin:0;
  }
  .bday-strip{
    text-align:center;
    font-family:'JetBrains Mono',monospace;
    font-size:12px;color:var(--text-mute);
    margin:10px 0 40px;
  }

  .pillars{
    display:grid;
    grid-template-columns:repeat(4,1fr);
    gap:18px;
  }
  @media (max-width:860px){
    .pillars{grid-template-columns:repeat(2,1fr);}
  }
  @media (max-width:480px){
    .pillars{grid-template-columns:1fr;}
  }

  .pillar{
    position:relative;
    background:linear-gradient(180deg, var(--paper) 0%, var(--paper-2) 100%);
    color:var(--text-dark);
    border-radius:8px;
    overflow:hidden;
    border:1px solid #b7a374;
    box-shadow:0 14px 30px -12px rgba(0,0,0,0.55);
    min-height:560px;
    display:flex;flex-direction:column;
  }
  .pillar-label{
    text-align:center;
    font-family:'JetBrains Mono',monospace;
    font-size:10.5px;
    letter-spacing:.14em;
    text-transform:uppercase;
    color:var(--seal);
    padding:12px 0 8px;
    border-bottom:1px solid rgba(165,52,43,0.25);
    position:relative;
    z-index:1;
  }
  .pillar-top{
    padding:12px 14px 14px;
    text-align:center;
    font-size:12px;
    color:#5c4d38;
    position:relative;z-index:1;
    height:96px;
    overflow:hidden;
  }
  .pillar-top .big-num{font-family:'JetBrains Mono',monospace;font-weight:700;font-size:14px;color:var(--text-dark);}
  .pillar-top .tietkhi-name-top{
    margin-top:5px;
    font-family:'Cormorant Garamond',serif;
    font-weight:700;
    font-size:14px;
    color:var(--seal);
  }
  .pillar-top .napam{
    margin-top:6px;
    font-style:italic;
    color:var(--jade);
    font-size:11.5px;
  }
  .hr-brass{height:1px;background:linear-gradient(90deg, transparent, var(--brass-dim), transparent);margin:0 14px;position:relative;z-index:1;}

  .tk2-caption{font-size:10.5px;color:#8a7856;text-align:center;margin-bottom:8px;}
  .tk2-names{
    display:grid;
    grid-template-columns:1fr auto 1fr;
    align-items:end;
    gap:6px;
    margin-bottom:9px;
  }
  .tk2-names .tk2-side{
    font-family:'JetBrains Mono',monospace;
    font-size:9.5px;
    color:#8a7856;
    letter-spacing:.02em;
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
  }
  .tk2-names .tk2-side.l{text-align:left;}
  .tk2-names .tk2-side.r{text-align:right;}
  .tk2-names .tk2-main{
    font-family:'Cormorant Garamond',serif;
    font-weight:700;
    font-size:15px;
    color:var(--seal);
    text-align:center;
    white-space:nowrap;
  }
  .tk2-track{
    position:relative;
    height:6px;
    border-radius:3px;
    background:rgba(107,92,66,0.18);
    margin:0 3px;
  }
  .tk2-seg-main{
    position:absolute;top:0;height:100%;
    background:var(--jade);
    border-radius:3px;
  }
  .tk2-tick{
    position:absolute;top:50%;
    width:2px;height:13px;
    background:#8a7856;
    transform:translate(-50%,-50%);
    border-radius:1px;
  }
  .tk2-dot{
    position:absolute;top:50%;
    width:11px;height:11px;
    border-radius:50%;
    background:var(--seal);
    border:2px solid var(--paper);
    box-shadow:0 0 0 2px rgba(165,52,43,0.22);
    transform:translate(-50%,-50%);
  }
  .tk2-dates{position:relative;height:14px;margin-top:7px;}
  .tk2-dates span{
    position:absolute;top:0;
    transform:translateX(-50%);
    font-family:'JetBrains Mono',monospace;
    font-size:9.5px;
    color:#8a7856;
    white-space:nowrap;
  }

  /* ===== Trụ tháng — timeline chi tiết (5 mốc: tiết đầu · 1/4 · khí giữa · 3/4 · tiết cuối) ===== */
  .tk3-names{position:relative;height:19px;margin-bottom:9px;}
  .tk3-name{
    position:absolute;top:0;
    font-family:'Cormorant Garamond',serif;
    font-weight:700;
    font-size:13px;
    white-space:nowrap;
  }
  .tk3-name.n-start{left:0;color:var(--seal);text-align:left;}
  .tk3-name.n-khi{transform:translateX(-50%);color:var(--jade);text-align:center;}
  .tk3-name.n-end{left:100%;transform:translateX(-100%);color:var(--seal);text-align:right;}
  .tk3-track{
    position:relative;
    height:6px;
    border-radius:3px;
    background:rgba(107,92,66,0.18);
    margin:0 3px;
  }
  .tk3-tick{position:absolute;top:50%;transform:translate(-50%,-50%);border-radius:1px;}
  .tk3-tick-major{width:2.5px;height:14px;background:var(--seal);}
  .tk3-tick-minor{width:1.5px;height:8px;background:#8a7856;opacity:.55;}
  .tk3-tick-khi{width:2.5px;height:14px;background:var(--jade);}
  .tk3-dates{position:relative;height:13px;margin-top:7px;}
  .tk3-dates span{
    position:absolute;top:0;
    transform:translateX(-50%);
    font-family:'JetBrains Mono',monospace;
    font-size:8.5px;
    color:#8a7856;
    white-space:nowrap;
  }
  .tk3-dates span:first-child{left:0;transform:translateX(0);}
  .tk3-dates span:last-child{transform:translateX(-100%);}
  .tk3-context{
    margin-top:9px;
    font-family:'JetBrains Mono',monospace;
    font-size:9px;
    color:#8a7856;
    text-align:center;
    letter-spacing:.01em;
  }

  .pillar-mid{
    flex:1;
    padding:16px 14px;
    text-align:center;
    position:relative;z-index:1;
  }
  .canchi-name{
    font-family:'Cormorant Garamond',serif;
    font-weight:700;
    font-size:30px;
    color:var(--text-dark);
    margin-top:6px;
  }
  .canchi-name .chi{color:var(--seal);}
  .tengod-main{
    font-size:11px;
    color:var(--jade);
    letter-spacing:.04em;
  }
  .tangcan-title{
    font-family:'JetBrains Mono',monospace;
    font-size:9.5px;
    letter-spacing:.1em;
    text-transform:uppercase;
    color:#8a7856;
    margin:10px 0 8px;
  }
  .tangcan-row{
    display:flex;
    justify-content:center;
    gap:8px;
    flex-wrap:wrap;
  }
  .tangcan-chip{
    background:rgba(81,113,92,0.09);
    border:1px solid rgba(81,113,92,0.35);
    border-radius:5px;
    padding:6px 8px;
    min-width:56px;
  }
  .tangcan-chip .tc-can{font-weight:700;font-size:13px;color:var(--text-dark);}
  .tangcan-chip .tc-role{font-size:8.5px;color:#8a7856;letter-spacing:.03em;}
  .tangcan-chip .tc-god{font-size:9.5px;color:var(--seal);margin-top:2px;}

  .pillar-bottom{
    padding:14px 14px 16px;
    border-top:1px solid rgba(165,52,43,0.25);
    font-size:11.5px;
    color:#6b5c42;
    text-align:center;
    position:relative;z-index:1;
    min-height:86px;
  }

  .tyzi-flag{
    position:absolute;top:8px;right:8px;
    background:var(--danger);
    color:#fff;
    font-size:8.5px;
    font-family:'JetBrains Mono',monospace;
    padding:3px 6px;
    border-radius:3px;
    z-index:2;
    letter-spacing:.05em;
  }

  /* ===== Confirm section ===== */
  .confirm-box{
    max-width:640px;
    margin:44px auto 0;
    text-align:center;
    background:var(--ink-2);
    border:1px solid var(--brass-dim);
    border-radius:8px;
    padding:26px 24px;
  }
  .confirm-box p{color:var(--text-mute);font-size:13.5px;margin:0 0 18px;}
  .confirm-actions{display:flex;gap:12px;justify-content:center;flex-wrap:wrap;}
  .btn-yes{
    background:var(--jade);color:#fff;border:none;
    padding:12px 22px;border-radius:5px;font-family:'Cormorant Garamond',serif;
    font-weight:600;font-size:16px;cursor:pointer;
    display:inline-flex;align-items:center;gap:8px;
  }
  .btn-no{
    background:transparent;color:var(--text-mute);border:1px solid var(--brass-dim);
    padding:12px 22px;border-radius:5px;font-size:13.5px;cursor:pointer;
    display:inline-flex;align-items:center;gap:8px;
  }
  .next-note{
    display:none;
    margin-top:18px;
    font-size:12.5px;
    color:var(--brass);
    border-top:1px dashed var(--brass-dim);
    padding-top:14px;
  }
  .next-note.show{display:block;}

  footer{
    text-align:center;
    margin-top:70px;
    color:var(--text-mute);
    font-size:11px;
    font-family:'JetBrains Mono',monospace;
    letter-spacing:.05em;
  }

  /* ===== Phần 2.2 – 2.7 : phân tích mở rộng ===== */
  .wide-card{max-width:900px;margin-top:26px;}
  .analysis-body{color:var(--text-mute);font-size:14px;line-height:1.7;}
  .analysis-body p{margin:0 0 12px;}
  .analysis-body b{color:var(--text-light);}
  .analysis-body h4{
    font-family:'Cormorant Garamond',serif;
    color:var(--brass);
    font-size:17px;
    margin:18px 0 8px;
  }
  .verdict-badge{
    display:inline-block;
    padding:6px 16px;
    border-radius:20px;
    font-family:'Cormorant Garamond',serif;
    font-weight:700;
    font-size:20px;
    margin-bottom:10px;
  }
  .verdict-vuong{background:rgba(165,52,43,0.18);color:var(--seal);}
  .verdict-nhuoc{background:rgba(81,113,92,0.18);color:var(--jade);}
  .verdict-trunghoa{background:rgba(198,160,76,0.18);color:var(--brass);}
  .bar-wrap{display:flex;height:14px;border-radius:7px;overflow:hidden;margin:10px 0 6px;background:rgba(255,255,255,0.06);}
  .bar-support{background:var(--jade);}
  .bar-drain{background:var(--seal);}
  .bar-legend{display:flex;justify-content:space-between;font-size:11px;color:var(--text-mute);font-family:'JetBrains Mono',monospace;}
  .elem-chip{
    display:inline-flex;align-items:center;gap:6px;
    padding:5px 12px;border-radius:20px;
    border:1px solid var(--brass-dim);
    font-size:13px;color:var(--text-light);
    margin:2px 4px 2px 0;
  }
  .data-table{width:100%;border-collapse:collapse;font-size:13px;margin-top:8px;}
  .data-table th,.data-table td{
    padding:8px 10px;text-align:center;
    border-bottom:1px solid rgba(198,160,76,0.15);
  }
  .data-table th{
    color:var(--brass);font-family:'JetBrains Mono',monospace;
    font-size:10.5px;letter-spacing:.05em;text-transform:uppercase;
  }
  .data-table td{color:var(--text-light);}
  .data-table tr.current-row td{background:rgba(165,52,43,0.16);font-weight:600;}
  .data-table tr.current-row td:first-child{border-left:3px solid var(--seal);}
  .table-scroll{overflow-x:auto;}
  .tag{
    display:inline-block;padding:3px 10px;border-radius:14px;
    font-size:11px;margin-right:6px;margin-bottom:4px;
  }
  .tag-good{background:rgba(81,113,92,0.2);color:#7fae8d;}
  .tag-bad{background:rgba(192,57,43,0.2);color:#e08a7d;}
  .tag-neutral{background:rgba(198,160,76,0.15);color:var(--brass);}
  .disclaimer{
    margin-top:16px;padding:12px 14px;
    border-left:3px solid var(--brass-dim);
    background:rgba(198,160,76,0.06);
    font-size:12px;color:var(--text-mute);
  }
  .category-block{
    border-bottom:1px dashed rgba(198,160,76,0.2);
    padding:14px 0;
  }
  .category-block:last-child{border-bottom:none;}
  .category-block h4{margin-top:0;}
  a{color:var(--brass);}
